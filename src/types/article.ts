export type Article = {
    id: string
    title: string
    category: string
    excerpt: string
    image: string
    publishedAt: string // e.g., "6 tháng trước"
    readTime: string // e.g., "10-15 phút đọc"
}

